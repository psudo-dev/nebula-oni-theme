type HexColor = `#${string}`;

export interface SyntaxPalette {
	pink: HexColor;
	magenta: HexColor;
	purple: HexColor;
	blue: HexColor;
	cyan: HexColor;
	green: HexColor;
	yellow: HexColor;
	white: HexColor;
	grey: HexColor;
	red: HexColor;
	brown: HexColor;
}
